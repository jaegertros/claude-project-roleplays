# 01_INDEX_MASTER

Project-wide routing table for the XML-tagged RAG card set. Detailed per-category indexes live in `02_INDEX_*.md`; card contents live in the category-prefixed files.

<master_rag_index project="The Last of Us / Allegheny" version="1">

<collection name="CAST" count="16">
Per-character prose profiles: appearance, voice, relationships, routine, body language, and portrayal.
Files begin with `CAST_` unless a source module uses a more specific prefix. See `02_INDEX_CAST.md` for the per-card index.
</collection>

<collection name="FACTION" count="4">
Faction profiles: group interests, resources, internal pressures, public posture, and response logic.
Files begin with `FACTION_` unless a source module uses a more specific prefix. See `02_INDEX_FACTIONS.md` for the per-card index.
</collection>

<collection name="HIDDEN" count="18">
Narrator-only secrets and gated reveals. Retrieval is permission to reason, not permission to reveal.
Files begin with `HIDDEN_` unless a source module uses a more specific prefix. See `02_INDEX_HIDDEN.md` for the per-card index.
</collection>

<collection name="LOCATION" count="16">
Location profiles: geography, sensory details, public layout, travel constraints, hazards, and local hooks.
Files begin with `LOCATION_` unless a source module uses a more specific prefix. See `02_INDEX_LOCATIONS.md` for the per-card index.
</collection>

<collection name="MISSION" count="19">
Opening and mission cards: active arcs, trigger logic, set-piece structure, and playable beats.
Files begin with `MISSION_` unless a source module uses a more specific prefix. See `02_INDEX_MISSIONS.md` for the per-card index.
</collection>

<collection name="TIMELINE" count="21">
Chronology cards: calendars, phase progression, off-screen sequence, and time-state continuity.
Files begin with `TIMELINE_` unless a source module uses a more specific prefix. See `02_INDEX_TIMELINE.md` for the per-card index.
</collection>

<collection name="TONE" count="8">
Style and prose discipline cards: voice registers, tonal constraints, forbidden moves, and response shape.
Files begin with `TONE_` unless a source module uses a more specific prefix. See `02_INDEX_TONE.md` for the per-card index.
</collection>

<collection name="UTILITY" count="10">
Practical reference cards: systems, resources, costs, logistics, and quick adjudication details.
Files begin with `UTILITY_` unless a source module uses a more specific prefix. See `02_INDEX_UTILITY.md` for the per-card index.
</collection>

<!-- No CAST/NPCMAP pairs were detected for this package. Use the collection indexes for routing. -->

</master_rag_index>
